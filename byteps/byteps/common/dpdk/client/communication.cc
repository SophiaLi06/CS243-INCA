#include "communication.h"

//void send_packet(DPDKcontext* dpdk_context, int packet_size, uint64_t offset);
void send_packet(int packet_size, uint64_t offset){
    // TODO: do dpdk transmission here
}